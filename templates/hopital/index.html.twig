{% extends 'base.html.twig' %}

{% block title %}Hello UserController!{% endblock %}

{% block body %}

    <h1 class="h1">Pharmacy list</h1>

    <a href="{{ url('hopital_add') }}" type="button" class="btn btn-primary color-white mb-3">
        Create a pharmacy
    </a>

    {% include 'Components/flashes.html.twig' %}

    <table class="table table-hover table-bordered table-striped">
        <thead>
        <tr>
            <th scope="col" class="table-dark">#</th>
            <th scope="col" class="table-dark">Name</th>
            <th scope="col" class="table-dark">Action</th>
        </tr>
        </thead>
        <tbody>
        {% for hopital in hopitaux %}
            <tr>
                <th scope="row">{{ hopital.id }}</th>
                <td>{{ hopital.name }}</td>
                <td>
                    <a href="{{ url('hopital_see', {id: hopital.id}) }}" type="button" class="btn btn-primary color-white">
                        See
                    </a>
                    <a href="{{ url('hopital_update', {id: hopital.id}) }}" type="button" class="btn btn-primary color-white">
                        Update
                    </a>
                    <a href="{{ url('hopital_remove', {id: hopital.id}) }}" type="button" class="btn btn-danger color-white">
                        Remove
                    </a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>


{% endblock %}
